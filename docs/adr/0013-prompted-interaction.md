# Interaction is a designed terminal: prompts for create/modify, a criterion line for search

The assignment lets the group decide interaction as long as US1–US11 hold. A prompted flow for create/modify matches “quick notes” and a 4-minute demo. Search still needs an explicit criterion language so `&&` `||` `!` and time comparisons are verifiable.

The screen shows Bound File, dirty flag, Alarm Alerts, Current Result, selected detail, and a prompt. Selection is by Id or by a row number of the current list (row numbers are not identity). `print` prints the selection; `print all` prints Current Result, not always the whole collection.

**Status**: accepted
